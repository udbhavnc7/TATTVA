[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests — fast, no I/O
    integration: Integration tests — require a running database
    property: Property-based tests using Hypothesis
